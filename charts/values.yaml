replicaCount: 1
image:
  repository: cactus-challenge12.azurecr.io/challenge

publicUrl: "cactus-challengedevops.duckdns.org"

specs:
  ingress:
    publicUrl: challengedevops.duckdns.org
    port: 3000
  service:
    name: http
    port: 3000
    protocol: TCP
    type: ClusterIP

env:
  URL: "cactus-challengedevops.duckdns.org:80"